using UnityEngine; // 네임 스페이스

public class VariableExample : MonoBehaviour // 클래스
{
    void Start() // 함수 (메서드)
    {
        int health;   // 체력 데이터를 저장할 변수
        int damage;   // 공격력 데이터를 저장할 변수
        float exp;    // 경험치를 저장할 변수
        double speed; // 속도를 저장할 변수
        // string name;  // 이름을 저장할 변수
        bool isDie;   // 생존 유무를 저장할 변수

        // string name = "정성훈";
        // int age = 27;
        // float height = 156.6f;
        // double weight = 57d;
        //
        // Debug.Log(name);
        // Debug.Log(age);
        // Debug.Log(height);
        // Debug.Log(weight);
    }
}
